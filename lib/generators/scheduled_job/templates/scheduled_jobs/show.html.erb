<%= render partial:"shared/view", locals: {
        rows: [
            ["ID",@scheduled_job.id],
            ["Name",@scheduled_job.name],
            ["Job Class",@scheduled_job.job_class],
            ["Cron",@scheduled_job.cron],
            ["Queue",@scheduled_job.queue],
            ["Active",@scheduled_job.active],
            ["Created At",@scheduled_job.created_at],
            ["Updated At",@scheduled_job.updated_at],
        ] ,
        header: "Scheduled Job ",   
        name: @scheduled_job.name,
        links: [
            
        ]
    } 
%>
<%= link_to 'Edit', [:edit,:admin,@scheduled_job], class:'btn btn-warning'%>
<%= link_to 'Delete', [:admin,@scheduled_job], method: :delete, data: { confirm: 'Are you sure?' }, class:'btn btn-danger' if can? :destroy, @scheduled_job %>